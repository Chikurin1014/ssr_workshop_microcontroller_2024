#include <Arduino.h>

// 最初に一回だけ実行される
void setup() {
    Serial.begin(9600);
}

// 繰り返し実行される
void loop() {
    // ここを埋めてください
    //   1から100までの数値を順番に出力する
    //   ただし、3の倍数のときは"Fizz"、5の倍数のときは"Buzz"、3の倍数かつ5の倍数のときは"FizzBuzz"と出力する
}
